import type { AdminProps } from "./types";

export default function AdminChains(_props: AdminProps) {
  return <section className="rounded-xl border border-white/5 bg-[#111] p-8 text-center text-zinc-400">Chains moderation will appear here when chains are available.</section>;
}